import { describe } from "vitest";

describe("Validate RoomsDB", () => {
	// No JSON validation tests anymore
	// because the JSON is not stored
	// in the repo. It has to be provided
	// by the user in the public folder
});
